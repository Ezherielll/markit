import 'conversion_executor.dart';

/// Stub untuk platform yang tidak didukung (tidak boleh terpakai).
ConversionExecutor createPlatformExecutor() {
  throw UnsupportedError('Platform tidak didukung.');
}
